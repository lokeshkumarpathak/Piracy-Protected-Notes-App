<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="12dp"
    android:layout_marginEnd="12dp"
    android:layout_marginTop="6dp"
    android:layout_marginBottom="6dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="3dp"
    app:cardBackgroundColor="#FFFFFF">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Title row: PDF icon + title + Downloaded badge -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="6dp">

            <ImageView
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:src="@android:drawable/ic_menu_agenda"
                android:layout_marginEnd="10dp"
                android:contentDescription="PDF" />

            <!-- Title takes remaining space -->
            <TextView
                android:id="@+id/tv_pdf_title"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textSize="17sp"
                android:textStyle="bold"
                android:textColor="#1A1A1A"
                android:maxLines="2"
                android:ellipsize="end" />

            <!-- Downloaded badge — visible only when file is cached locally -->
            <TextView
                android:id="@+id/tv_downloaded_badge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Downloaded"
                android:textSize="10sp"
                android:textColor="#FFFFFF"
                android:background="#388E3C"
                android:paddingStart="8dp"
                android:paddingEnd="8dp"
                android:paddingTop="3dp"
                android:paddingBottom="3dp"
                android:layout_marginStart="8dp"
                android:visibility="gone" />

        </LinearLayout>

        <!-- Date + file size -->
        <TextView
            android:id="@+id/tv_date_size"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textColor="#9E9E9E"
            android:layout_marginBottom="12dp" />

        <!-- Per-card download progress bar — gone by default -->
        <ProgressBar
            android:id="@+id/progress_download"
            style="@android:style/Widget.ProgressBar.Horizontal"
            android:layout_width="match_parent"
            android:layout_height="8dp"
            android:layout_marginBottom="10dp"
            android:visibility="gone"
            android:indeterminate="false"
            android:max="100"
            android:progressTint="#1565C0"
            android:progressBackgroundTint="#E3F2FD" />

        <!-- Single action button: "Download" or "Open" depending on state -->
        <Button
            android:id="@+id/btn_action"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Download"
            android:textColor="#FFFFFF"
            android:backgroundTint="#1565C0" />

    </LinearLayout>

</androidx.cardview.widget.CardView>