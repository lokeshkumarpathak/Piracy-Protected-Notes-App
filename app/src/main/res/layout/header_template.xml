<?xml version="1.0" encoding="utf-8"?>
<!--
    ╔══════════════════════════════════════════════════════╗
    ║  REUSABLE HEADER TEMPLATE  —  header_template.xml   ║
    ╠══════════════════════════════════════════════════════╣
    ║  INCLUDE IN ANY SCREEN XML:                          ║
    ║                                                      ║
    ║  <include                                            ║
    ║      layout="@layout/header_template"                ║
    ║      android:id="@+id/header"                        ║
    ║      android:layout_width="0dp"                      ║
    ║      android:layout_height="wrap_content"            ║
    ║      app:layout_constraintTop_toTopOf="parent"       ║
    ║      app:layout_constraintStart_toStartOf="parent"   ║
    ║      app:layout_constraintEnd_toEndOf="parent" />    ║
    ║                                                      ║
    ║  SET TITLE IN YOUR ACTIVITY:                         ║
    ║  findViewById<TextView>(R.id.tv_header_title)        ║
    ║      .text = "Your Screen Name"                      ║
    ║                                                      ║
    ║  CUSTOM BACK (optional):                             ║
    ║  findViewById<ImageButton>(R.id.btn_back)            ║
    ║      .setOnClickListener { your logic }              ║
    ╚══════════════════════════════════════════════════════╝
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/header_background"
    android:paddingTop="4dp"
    android:paddingBottom="4dp"
    android:elevation="8dp">

    <!-- Back button with circular ripple -->
    <ImageButton
        android:id="@+id/btn_back"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:layout_marginStart="12dp"
        android:src="@drawable/ic_arrow_back"
        android:background="@drawable/header_back_btn_bg"
        android:contentDescription="Back"
        android:scaleType="fitCenter"
        android:padding="10dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <!-- Screen title — centred between back button and mirror spacer -->
    <TextView
        android:id="@+id/tv_header_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp"
        android:layout_marginTop="18dp"
        android:layout_marginBottom="18dp"
        android:text="Screen Title"
        android:textColor="#FFFFFF"
        android:textSize="17sp"
        android:textStyle="bold"
        android:gravity="center"
        android:letterSpacing="0.05"
        android:fontFamily="sans-serif-medium"
        app:layout_constraintStart_toEndOf="@+id/btn_back"
        app:layout_constraintEnd_toStartOf="@+id/header_spacer"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <!-- Mirror spacer — keeps title truly centred -->
    <Space
        android:id="@+id/header_spacer"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:layout_marginEnd="12dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <!-- Subtle bottom divider -->
    <View
        android:layout_width="0dp"
        android:layout_height="1dp"
        android:background="#22FFFFFF"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>