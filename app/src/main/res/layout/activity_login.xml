<?xml version="1.0" encoding="utf-8"?>
<!--
    Root FrameLayout lets the loading overlay sit on top of the ScrollView,
    so it is always truly centred on screen regardless of scroll position.
-->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_tiled">

    <!-- ── Scrollable login form ────────────────────────────────────────── -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true"
        android:background="@android:color/transparent">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:paddingTop="48dp"
            android:paddingBottom="32dp">

            <!-- App logo -->
            <ImageView
                android:id="@+id/iv_logo"
                android:layout_width="96dp"
                android:layout_height="96dp"
                android:layout_marginBottom="12dp"
                android:src="@drawable/logo"
                android:scaleType="centerCrop"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />

            <!-- App title -->
            <TextView
                android:id="@+id/tv_title"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:text="PPN App Login"
                android:textSize="22sp"
                android:textStyle="bold"
                android:textColor="#1A1A1A"
                android:gravity="center"
                android:layout_marginBottom="32dp"
                app:layout_constraintTop_toBottomOf="@+id/iv_logo"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />

            <!-- ════ SECTION 1 — EMAIL ════ -->

            <EditText
                android:id="@+id/email"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:layout_marginEnd="8dp"
                android:hint="Email (@nitkkr.ac.in)"
                android:inputType="textEmailAddress"
                android:ems="10"
                app:layout_constraintTop_toBottomOf="@+id/tv_title"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toStartOf="@+id/verifyEmailBtn" />

            <Button
                android:id="@+id/verifyEmailBtn"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Send OTP"
                android:textSize="12sp"
                app:layout_constraintTop_toTopOf="@+id/email"
                app:layout_constraintBottom_toBottomOf="@+id/email"
                app:layout_constraintEnd_toEndOf="parent" />

            <EditText
                android:id="@+id/email_otp_field"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:layout_marginEnd="8dp"
                android:hint="Email OTP (6 digits)"
                android:inputType="number"
                android:maxLength="6"
                android:visibility="gone"
                app:layout_constraintTop_toBottomOf="@+id/email"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toStartOf="@+id/confirmEmailOtpBtn" />

            <Button
                android:id="@+id/confirmEmailOtpBtn"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Confirm"
                android:textSize="12sp"
                android:enabled="false"
                android:visibility="gone"
                app:layout_constraintTop_toTopOf="@+id/email_otp_field"
                app:layout_constraintBottom_toBottomOf="@+id/email_otp_field"
                app:layout_constraintEnd_toEndOf="parent" />

            <LinearLayout
                android:id="@+id/layout_email_verified_row"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginTop="4dp"
                android:gravity="center_vertical"
                android:visibility="visible"
                app:layout_constraintTop_toBottomOf="@+id/email_otp_field"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent">

                <TextView
                    android:id="@+id/tv_email_verified"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="✓ Email verified"
                    android:textColor="#388E3C"
                    android:textStyle="bold"
                    android:textSize="12sp"
                    android:visibility="gone" />

                <Button
                    android:id="@+id/btn_edit_email"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="✎ Edit Email"
                    android:textSize="11sp"
                    android:textColor="#1565C0"
                    android:backgroundTint="@android:color/transparent"
                    android:paddingStart="0dp"
                    android:paddingEnd="0dp" />

            </LinearLayout>

            <!-- Divider -->
            <View
                android:id="@+id/divider1"
                android:layout_width="0dp"
                android:layout_height="1dp"
                android:background="#E0E0E0"
                android:layout_marginTop="16dp"
                app:layout_constraintTop_toBottomOf="@+id/layout_email_verified_row"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />

            <!-- ════ SECTION 2 — PHONE ════ -->

            <EditText
                android:id="@+id/phone_field"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:layout_marginEnd="8dp"
                android:hint="Phone (+91XXXXXXXXXX)"
                android:inputType="phone"
                android:ems="10"
                app:layout_constraintTop_toBottomOf="@+id/divider1"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toStartOf="@+id/getOtp" />

            <Button
                android:id="@+id/getOtp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Send OTP"
                android:textSize="12sp"
                app:layout_constraintTop_toTopOf="@+id/phone_field"
                app:layout_constraintBottom_toBottomOf="@+id/phone_field"
                app:layout_constraintEnd_toEndOf="parent" />

            <EditText
                android:id="@+id/otp_field"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:layout_marginEnd="8dp"
                android:hint="Phone OTP (6 digits)"
                android:inputType="number"
                android:maxLength="6"
                android:visibility="gone"
                app:layout_constraintTop_toBottomOf="@+id/phone_field"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toStartOf="@+id/confirmPhoneOtpBtn" />

            <Button
                android:id="@+id/confirmPhoneOtpBtn"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Confirm"
                android:textSize="12sp"
                android:enabled="false"
                android:visibility="gone"
                app:layout_constraintTop_toTopOf="@+id/otp_field"
                app:layout_constraintBottom_toBottomOf="@+id/otp_field"
                app:layout_constraintEnd_toEndOf="parent" />

            <LinearLayout
                android:id="@+id/layout_phone_verified_row"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginTop="4dp"
                android:gravity="center_vertical"
                android:visibility="visible"
                app:layout_constraintTop_toBottomOf="@+id/otp_field"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent">

                <TextView
                    android:id="@+id/tv_phone_verified"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="✓ Phone verified"
                    android:textColor="#388E3C"
                    android:textStyle="bold"
                    android:textSize="12sp"
                    android:visibility="gone" />

                <Button
                    android:id="@+id/btn_edit_phone"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="✎ Edit Phone"
                    android:textSize="11sp"
                    android:textColor="#1565C0"
                    android:backgroundTint="@android:color/transparent"
                    android:paddingStart="0dp"
                    android:paddingEnd="0dp" />

            </LinearLayout>

            <!-- ════ SECTION 3 — ACTIONS ════ -->

            <Button
                android:id="@+id/login"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="24dp"
                android:text="Login"
                android:backgroundTint="#1565C0"
                android:textColor="#FFFFFF"
                app:layout_constraintTop_toBottomOf="@+id/layout_phone_verified_row"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />

            <ProgressBar
                android:id="@+id/login_progress"
                style="?android:attr/progressBarStyleSmall"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:visibility="gone"
                app:layout_constraintTop_toBottomOf="@+id/login"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />

            <Button
                android:id="@+id/resend_request_btn"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:text="Resend Approval Request"
                android:visibility="gone"
                app:layout_constraintTop_toBottomOf="@+id/login_progress"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />

        </androidx.constraintlayout.widget.ConstraintLayout>
    </ScrollView>

    <!-- ── Full-screen loading overlay — logo + spinner + text ─────────── -->
    <LinearLayout
        android:id="@+id/loading"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:background="@drawable/bg_tiled"
        android:visibility="gone">

        <ImageView
            android:layout_width="112dp"
            android:layout_height="112dp"
            android:src="@drawable/logo"
            android:scaleType="centerCrop"
            android:layout_marginBottom="32dp" />

        <ProgressBar
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginBottom="16dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Loading..."
            android:textSize="16sp"
            android:textColor="#555555"
            android:fontFamily="sans-serif-medium"
            android:letterSpacing="0.05" />

    </LinearLayout>

</FrameLayout>